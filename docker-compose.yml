services:
  dependencies:
    build: 
      context: .
      dockerfile: dependencies.Dockerfile
    image: dependencies
  webserver:
    build: src/webserver/
    image: webserver
    ports:
      - "${SALT_WEB_PORT}:443"
    environment:
      - SALT_WEB_PORT=443
